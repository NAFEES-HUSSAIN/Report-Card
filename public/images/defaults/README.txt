Put your default staff profile photo here.

Required file name (must match User::profilePhotoUrl()):
  user-avatar.png

Uploaded user photos are NOT stored here — they go to storage/app/public/avatars
and only the path is saved in users.avatar_path.

After replacing this file, hard-refresh the browser (Ctrl+F5).
